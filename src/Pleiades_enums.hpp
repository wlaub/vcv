
    enum ParamIds {
        ENUMS(PARAM_CENTER,1),
        ENUMS(PARAM_CONFIG,3),
        ENUMS(PARAM_MODE,7),
        ENUMS(PARAM_STEP,7),
        ENUMS(PARAM_SAVE,1),
        ENUMS(PARAM_LOAD,1),
        NUM_PARAMS
    };
    enum InputIds {
        ENUMS(INPUT_ANALOG,1),
        ENUMS(INPUT_CLOCK,1),
        NUM_INPUTS
    };
    enum OutputIds {
        ENUMS(OUTPUT_OUT,8),
        NUM_OUTPUTS
    };
    /*enum LightIds {

        NUM_LIGHTS
    };*/
    
